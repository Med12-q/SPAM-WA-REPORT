{
  "buildCommand": "pnpm --filter @workspace/spam-wa-report run build",
  "outputDirectory": "artifacts/spam-wa-report/dist/public",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "framework": null,
  "env": {
    "PORT": "3000",
    "BASE_PATH": "/",
    "ENABLE_EXPERIMENTAL_COREPACK": "1"
  }
}
